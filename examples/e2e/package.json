{
  "name": "padmin-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end tests for the padmin compose stack (palpo + pasion + element + padmin)",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "reset": "node scripts/reset-stack.mjs",
    "stack:up": "node scripts/reset-stack.mjs up",
    "stack:down": "node scripts/reset-stack.mjs down"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
